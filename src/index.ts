// @ts-ignore
import {
  call,
  request,
  publish,
  subscribe,
  queue,
  listen,
} from "./call-service";

import ServiceDiscovery from "./service-discovery";

export default {
  call,
  request,
  publish,
  subscribe,
  queue,
  listen,

  ServiceDiscovery,
};
