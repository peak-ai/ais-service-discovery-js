'use strict';

const {
  call,
  request,
  publish,
  subscribe,
  queue,
  listen,
} = require('./call-service');

module.exports = {
  call,
  request,

  publish,
  subscribe,

  queue,
  listen,

};
